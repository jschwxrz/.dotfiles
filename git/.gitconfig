[filter "lfs"]
        clean = git-lfs clean -- %f
        smudge = git-lfs smudge -- %f
        process = git-lfs filter-process
        required = true
[user]
        name = Jona Schwarz
        email = jonaschwarz1@gmail.com
        signingkey = ~/.ssh/id_ed25519

[core]
    pager = delta

[delta]
    line-numbers = true
